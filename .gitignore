/target

# Local secrets (SIP trunk credentials, Gemini API key) — never commit
.env
kutsu.toml
/logs/

*.log
uplink-dump/
downlink-dump/
# Per-call runtime output (audio dumps + transcripts)
/dumps/
/transcripts/

# Local scratch + synthetic test audio (dev only)
/scratchpad/
/synth-test/

# Local AI/agent tooling — not part of the project
/.claude/
/.superpowers/
/docs/superpowers/
CLAUDE.md
.mcp.json

# Internal working notes (kept locally, not part of the OSS docs)
/docs/backlog.md

# AMD corpus — private call recordings (voices, real numbers); never commit
/amd-corpus/
